const fs = require('fs')
const path = require('path')

const libDir = path.join('lib')
if (!fs.existsSync(libDir)) fs.mkdirSync(libDir, { recursive: true })
fs.copyFileSync('compressImage.ts', path.join('lib', 'compressImage.ts'))
console.log('Installed: lib/compressImage.ts')
console.log('\nNow run these four fix scripts in order:')
console.log('node fix-memories-compression.js')
console.log('node fix-composer-compression.js')
console.log('node fix-hangoutcard-compression.js')
console.log('node fix-postcomments-compression.js')
console.log('\nThen: npm run build')
